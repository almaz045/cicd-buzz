sudo: required

services:
  - docker

language: python

script:
  - python -m pytest -v

after_success:
  — sh .travis/deploy_dockerhub.sh
  — test “$TRAVIS_BRANCH” = “master” && sh .travis/deploy_heroku.sh

deploy:
  skip_cleanup: true
  provider: heroku
  api_key:
    secure: 2e30c7fc-4902-4a18-9114-28cf820f2299
 
