sudo: required

services:
  - docker

language: python

script:
  - python -m pytest -v

after_success:
  — sh .travis/deploy_dockerhub.sh
  — test “$TRAVIS_BRANCH” = “master” && sh .travis/deploy_heroku.sh

deploy:  
  provider: heroku
  app: almazvakh
  on:
    branch: master
  api_key:
    secure: a4df57d9-6dc0-4737-a114-07937f232edd
  skip_cleanup: true
 
